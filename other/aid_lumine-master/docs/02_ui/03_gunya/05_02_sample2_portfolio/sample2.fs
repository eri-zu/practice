
      
      
      precision highp float;
      precision highp int;
    
      #define GLSLIFY 1
uniform vec3 uColor;
uniform float uOpacity;
varying vec2 vUv;

void main() {
  vUv;
  gl_FragColor = vec4(uColor, uOpacity);
}
    